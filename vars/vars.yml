# user information
USER_ID: 1026
GROUP_ID: 100

# Transmission Env variables
TRANSMISSION_USER: transmission
TRANSMISSION_PASSWORD: password

# transmission volume mapping
TRANSMISSION_CONFIG_PATH: ~/transmission/config
TRANSMISSION_DOWNLOADS_PATH: /volume1/NAS/downloads
TRANSMISSION_WATCH_PATH: /volume1/NAS/torrents-watch

# transmission port mapping
TRANSMISSION_WEB_PORT: 9091
TRANSMISSION_TCP_PORT: 51413
TRANSMISSION_UDP_PORT: 51413

# pyload volume mapping
PYLOAD_CONFIG_PATH: ~/pyload/config
PYLOAD_DOWNLOAD_PATH: /volume1/NAS/downloads

# pyload port mapping
PYLOAD_WEB_PORT: 8000
PYLOAD_AUX_POR: 7227
